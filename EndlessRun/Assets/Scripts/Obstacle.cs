using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;

/// <summary>
/// this script handles the player death when touching the obstacle
/// it will be attached to the player script
/// </summary>

public class ObstacleWall : MonoBehaviour

{
   
   //list of all obstacles asigned via Inpector
   //[SerializeField] private  List<GameObject> obstacles;
   //[SerializeField] private GameObject Player;
   // void OnCollisionEnter(Collision collision)
   // {
        //checks if player collided with any obstacles in the list, if so it will use die method
     //   foreach( GameObject obstacle in obstacles)
    //    if (collision.gameObject == obstacle ){
       //     Die();
            //exits after first collision
      //      return;

      //  }
   // }
  //  void Die()
  //  {
 //       // Call the death function
  //      Debug.Log("Player died!");
        // Destroy the player GameObject
  //      Destroy(Player);
   // }
  
}
